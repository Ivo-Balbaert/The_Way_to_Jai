- Memory management
	This memory has to be freed. There is no garbage collector in Jai:  except for stack variables, which are freed automatically when their scope (lifetime) ends, the developer must manually free the memory.
- Types - Any

- highlighting code?



bool in conditions:
If var is a non-zero value, it tests as true in conditions, and !var is equal to false.

if n % 2 == 0   print("n is an even number");
if (n & 1) == 0 // this is true for n with even value

Date type ?

LATER:
- Debugging